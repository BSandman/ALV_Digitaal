{
  "name": "alv-digitaal-app",
  "version": "0.1.0",
  "private": true,
  "description": "Digitaal ALV-eigenaarportaal — beheer + mobiele eigenaarstoegang (single-process, MySQL/MariaDB state).",
  "type": "module",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test ../tests/architecture-gate.test.mjs ../tests/seed-preparer.test.mjs ../tests/pii-scan.test.mjs",
    "gate:architecture": "node ../scripts/architecture-gate.mjs",
    "build:release": "node ../scripts/build-release.mjs"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  },
  "devDependencies": {}
}
