{
  "name": "alv-digitaal-app",
  "version": "0.1.0",
  "private": true,
  "description": "Digitaal ALV-eigenaarportaal — beheer + mobiele eigenaarstoegang (single-process, MySQL/MariaDB state).",
  "type": "module",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  },
  "devDependencies": {}
}
